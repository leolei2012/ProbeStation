# ProbeStation 测试总报告（汇总）

> 整理时间：2026-08-16 11:08:04（+08:00）
> 范围：`http://localhost:8080/` Web UI + REST，`8081` MCP，`8502` 从站模拟器
> 本文件为五轮测试的总索引，含**问题全生命周期追踪**（发现 → 修复 → 验证）。

---

## 一、测试轮次时间线

| 轮次 | 报告文件 | 时间（+08:00） | 内容 | 关键结论 |
|---|---|---|---|---|
| 1 | `测试报告.md` | 08-15 18:53:21 | 首轮全量（加载/功能/控制台/响应式/性能） | 发现 9 个问题（P0×1、P1×3、P2×4、P3×1） |
| 2 | `复测报告.md` | 08-15 19:56:28 | 首轮 9 问题复测 | 9 个全部修复；追加发现「新建设备两个按钮」 |
| 3 | `规格符合性测试报告.md` | 08-15 23:16:16 | 按产品规格书逐项符合性 | REST 18 项通过；发现 MCP 会话 P0 + 4 处规格漂移 |
| 4 | `复测报告-第3轮.md` | 08-16 11:02:09 | MCP 修复 + RTU 新功能 | MCP P0 修复；RTU 基本可用，发现侧栏显示问题 |
| 5 | `复测报告-第4轮.md` | 08-16 11:05:27 | 侧栏 RTU 修复回归 | 侧栏修复；全绿 |

---

## 二、问题全生命周期追踪

### 2.1 代码问题

| ID | 问题 | 级别 | 发现 | 状态 | 修复/验证 |
|---|---|---|---|---|---|
| 1 | 历史数据多分组时间戳错位（另一组永远 `—`） | 🔴 P0 | 轮1 | ✅ 已修复 | 轮2（poller 同周期共享时间戳） |
| 2 | 删除设备/分组无确认 | 🟠 P1 | 轮1 | ✅ 已修复 | 轮2（window.confirm） |
| 3 | 「新建设备」入口隐蔽 | 🟠 P1 | 轮1 | ✅ 已修复 | 轮2（可见按钮 + 空状态 CTA） |
| 4 | 写寄存器无校验/无反馈 | 🟠 P1 | 轮1 | ✅ 已修复 | 轮2（空值/NaN 校验 + 成功提示） |
| 5 | 移动端无响应式 | 🟡 P2 | 轮1 | ✅ 已修复 | 轮2（@media 抽屉化） |
| 6 | 缺 favicon → 404 | 🟡 P2 | 轮1 | ✅ 已修复 | 轮2（内联 SVG） |
| 7 | 静态资源未压缩 | 🟡 P2 | 轮1 | ✅ 已修复 | 轮2（@fastify/compress，zstd） |
| 8 | API 无统一错误处理 + 历史静默吞错 | 🟡 P2 | 轮1 | ✅ 已修复 | 轮2（error 态 + .catch） |
| 9 | 历史 `query` useCallback 依赖不完整 | ⚪ P3 | 轮1 | ✅ 已修复 | 轮2（补 selected） |
| 10 | MCP 会话只能服务一次，「already initialized」卡死 | 🔴 P0 | 轮3 | ✅ 已修复 | 轮4（每会话独立 McpServer+transport） |
| 11 | 侧栏设备列表对 RTU 显示空端口 `:502` | 🟡 P2 | 轮4 | ✅ 已修复 | 轮5（RTU 显示 serialPath） |
| **12** | **「新建设备」两个按钮冗余（工作区行裸「＋」+ 设备列表「＋ 新建设备」）** | 🟡 P2 | 轮2 | ❌ **仍未修复** | — |

> 注：ID 12 由轮 2 补充发现（`App.tsx` 第 175 行 `ws-row-add` 与第 365 行 `new-device-btn` 并存），本轮再次实测确认两处按钮均仍存在。建议删除第 175 行的裸「＋」（及其 `onAdd` prop），保留第 365 行的文字按钮。

### 2.2 文档漂移（产品规格书 `docs/product/02-产品规格书.md`，产品侧维护，待更新）

| ID | 位置 | 规格书写 | 实际 |
|---|---|---|---|
| D1 | §5.15/§6.3 | 「12 个工具」含 `read_all` | 实际 **14 个工具**：`read_all`→`get_device_snapshot`，新增 `get_device_health`/`list_alarm_rules` |
| D2 | §5.5 | `poll_data(object_id, register_id, …)` | 实际（§7.2、代码）已改为 `address`，自相矛盾 |
| D3 | §5.6/§9/§8 | 「历史曲线图未实现」 | 实际已有「曲线」Tab 并渲染 SVG |
| D4 | §5.6 | 工具栏「暂停 / 导出 / 删除」 | 实际「连接/断开 / 编辑 / 删除」，导出在历史页 |
| D5 | §3.1/§5.1/§5.4/§9/§11 | RTU 串口「规划中/未实现」 | 实际已实现（`transport: tcp|rtu` + 串口参数 + SerialDriver + 串行轮询） |

---

## 三、当前程序健康状态（轮 5 终态）

| 维度 | 结果 |
|---|---|
| REST 符合性 | ✅ 18/18 |
| MCP 符合性（14 工具 + PRD 验收） | ✅ 10/10 |
| MCP 会话复用（A/B/C 连续连接） | ✅ 各 14 工具 |
| RTU UI 全流程 | ✅ 11/11 |
| 实时表/历史/曲线 | ✅ 正常 |
| console 错误/告警 | ✅ 0 |
| 性能（TTFB 3ms / load 40ms） | ✅ 正常 |

**核心链路（采集→存储→可视化→写值→告警→导出→MCP 读写→RTU 串口）已完整打通。** 除 1 个待修的按钮冗余（ID 12）外，无代码缺陷。

---

## 四、当前待办

1. 🟡 **代码**：删除「新建设备」冗余按钮（ID 12，`App.tsx` 第 175 行裸「＋」）。
2. 📝 **产品侧文档**：更新产品规格书 5 处漂移（D1–D5）。

---

## 五、测试资产清单

**脚本**（`test/`）：
- `test.js` / `retest.js` — 浏览器全量/复测
- `conformance.ts` — REST + MCP 规格符合性（28 项断言）
- `mcp-reconnect.ts` — MCP 会话复用回归
- `rtu-regression.js` — RTU UI + 回归
- `verify.js` / `repro-history-alignment.js` / `check-history-recent.js` / `check-404.js` — 专项排查

**截图**（`test/screenshots/`）：桌面/平板/手机/深色主题/历史对齐/曲线/RTU 弹窗等 13 张。

**报告**（`docs/test/`）：5 份分轮报告 + 本总报告。
