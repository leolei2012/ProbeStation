# ProbeStation 复测报告（第 4 轮 · 侧栏 RTU 显示修复回归）

> 测试时间：2026-08-16 11:05:27（+08:00，测试脚本完成时刻）
> 测试对象：运行中的程序（8080 Web+REST、8081 MCP、8502 从站）
> 本轮范围：上轮「侧栏 RTU 显示空端口」修复验证 + 全量回归
> 测试脚本：`test/rtu-regression.js`、`test/conformance.ts`、`test/mcp-reconnect.ts`

---

## 一、结论

| 项 | 结果 |
|---|---|
| 🟡 上轮：侧栏 RTU 设备显示空端口 `:502` | ✅ **已修复** |
| RTU UI + 回归（浏览器） | ✅ **11/11 通过** |
| 规格符合性（REST + MCP） | ✅ **28/28 通过** |
| MCP 会话复用回归 | ✅ **A/B/C 三次连接全部成功** |
| 新增问题 | 无 |

本轮开发改动全部验证通过，无回归、无新问题。

---

## 二、修复验证

### 侧栏 RTU 显示（上轮 🟡 → 已修）

- `apps/web/src/App.tsx` 第 371 行由固定 `{ip}:{port}` 改为 `{d.transport === 'rtu' ? (d.serialPath || 'RTU') : (d.ip + ':' + d.port)}`，与主视图（第 428 行）一致。
- 浏览器实测：创建 RTU 设备后，侧栏设备项正确显示串口路径 `COM7`（上轮显示 `:502`）。

## 三、回归结果

| 测试 | 结果 |
|---|---|
| RTU UI 全流程（加载/弹窗/TCP-RTU 切换/串口字段/创建/显示/删除） | ✅ 11/11 |
| 设备1 实时表 2 组 / 历史查询 200 行 / 曲线 20 条折线 | ✅ |
| console 错误/告警 | ✅ 0 |
| REST 符合性（health/工作区/设备/组/寄存器/快照/历史/导出/规则/日志/写值/启停） | ✅ 18/18 |
| MCP 符合性（14 工具 + PRD 验收） | ✅ 10/10 |
| MCP 连续连接 A/B/C | ✅ 各 14 工具 |

---

## 四、遗留（非开发职责，供产品侧跟进）

产品规格书 `docs/product/02-产品规格书.md` 仍把 RTU 标记为「规划中（未实现）」（§3.1 第 54 行、§5.4 第 117 行、§9 第 251 行、§11 第 261 行），而开发侧已在 `docs/develop/01-变更记录.md` 完成「P0 RTU 串口支持」并验证。规格书（产品经理维护）需同步更新为「已实现」。

## 五、测试产物

- `test/rtu-regression.js`（RTU UI + 回归，11 项断言）
- `test/conformance.ts`（REST+MCP 符合性，28 项）
- `test/mcp-reconnect.ts`（MCP 会话复用）
- `test/screenshots/rtu-modal.png`
